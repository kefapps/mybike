# RideMock PlayMode Smoke

Ce dossier reserve le smoke PlayMode MYB-11. Le test executable sera ajoute
apres creation de `Assets/Scenes/RideMock.unity` par Unity Editor.

Contrat a verifier:

- scene chargee;
- route renderer visible;
- camera active;
- HUD present;
- input mock present;
- fog/depth actif.
