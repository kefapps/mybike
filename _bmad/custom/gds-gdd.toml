[workflow]

on_complete = """
After the GDD artifact is finalized, compare it with _bmad-output/linear-sync.md.
If the GDD changed project scope, MVP boundaries, or handoff expectations, propose
a Linear sync for team MYB / project "Echappee 3D - Vertical Slice Mock".
Do not create or update Linear records without explicit user confirmation.
If the user approves, update Linear documents/issues and then update
_bmad-output/linear-sync.md with returned IDs and URLs.
"""
