[workflow]

on_complete = """
After epics and stories are finalized, compare the generated artifact with
_bmad-output/linear-sync.md. Automatically sync non-destructive updates to
existing Linear records in team MYB / project "Echappee 3D - Vertical Slice Mock"
while preserving the constrained scope:
exactly one MVP epic and three to six vertical-slice stories unless the user
explicitly approves a scope change.
Then update _bmad-output/linear-sync.md with returned IDs, URLs, and status.
Ask for explicit confirmation before creating new projects/epics/stories,
archiving/deleting/renaming canonical trackers, changing ownership, or expanding
beyond the current one MVP epic and five vertical-slice stories.
"""
